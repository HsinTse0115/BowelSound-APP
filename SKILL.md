---
name: multi-agent-dev
description: 多 Agent 並行專案開發框架。當用戶想要開發一個新專案、建立程式架構、或是需要系統性地組織並分工開發任務時，使用此 skill。觸發情境包括：「幫我開發一個專案」、「我想建立一個系統」、「幫我規劃並開發 XX 功能」、「多 agent 開發」、「並行開發」。此 skill 會引導主控台 AI 完成從需求釐清、文件生成、模組拆分、子 Agent 派發、到測試與修正的完整開發流程。
---

# Multi-Agent 並行開發框架

## 核心原則

- **主控台** = 當前對話的 Claude（負責協調、不直接寫程式）
- **子 Agent** = 由主控台透過 Claude Code 派發的獨立 agent（各自負責一個模組）
- **最小上下文** = 每個 agent 只讀自己需要的 MD，減少 token 消耗
- **透明開發** = 用戶隨時知道每個 agent 在做什麼

---

## ⚠️ 最高優先規則：不猜測、不假設

**主控台與所有子 Agent 必須嚴格遵守：**

> 任何不確定的地方，不論大小，都必須先問用戶，不得自行猜測或假設。

這條規則適用於所有情境，包括但不限於：

- 技術選型不明確時 → 問，不要猜
- 功能邊界模糊時 → 問，不要自行補充
- 輸入/輸出格式未說明時 → 問，不要假設
- 有多種可能的實作方式時 → 列出選項問用戶
- 測試情境不夠明確時 → 問清楚再寫
- 模組介面不確定時 → 問，不要自己定義

**「感覺合理」不是行動的依據，用戶的明確確認才是。**

子 Agent 若在開發過程中遇到規格不清楚的地方，必須停下來回報主控台，由主控台詢問用戶後再繼續，而不是自行補充邏輯。

---

## 流程總覽

```
Phase 1: 需求釐清      → project_overview.md
Phase 2: 詳細規格      → project_detail.md + modules/*.md
Phase 3: 相依分析      → 決定執行順序
Phase 4: 並行開發      → 子 Agent 各自開發
Phase 5: 測試與修正    → 主控台驗證，子 Agent 修正
Phase 6: 完成確認      → 全部通過才算完成
```

---

## Phase 1：需求釐清

### 1-1 詢問基本資訊

主控台**必須先問清楚**以下問題，不要假設任何細節：

**專案基本資訊：**
- 專案名稱與用途是什麼？
- 主要使用者是誰？
- 預計部署環境（本地、雲端、容器）？

**技術選型：**
- 主要程式語言（Python / TypeScript / JavaScript / 混合）？
- 需要哪些框架或函式庫？
- 是否有資料庫？用哪種？
- 是否需要 API？REST / GraphQL / 其他？
- 是否需要前端？

**功能需求：**
- 核心功能有哪些（列舉每一項）？
- 有哪些非功能需求（效能、安全性、可擴展性）？
- 有沒有特別的限制或規則？

**開發限制：**
- 有無時間限制？
- 有無特定的程式碼風格或規範要求？
- 是否需要支援多語言 / 國際化？

> 💡 **原則**：寧可多問，不要假設。所有問題一次列出，讓用戶統一回答，避免來回多次。

### 1-2 生成 project_overview.md

收到回答後，生成並存至 `docs/project_overview.md`：

```markdown
# [專案名稱] - 概覽

## 專案描述
[用途、目標用戶、部署環境]

## 技術棧
- 語言：
- 框架：
- 資料庫：
- 其他：

## 核心功能清單
1. [功能 1]
2. [功能 2]
...

## 非功能需求
- [效能、安全性等]

## 開發限制
- [規範、時間等]
```

### 1-3 根據 MD 補充問題

生成 MD 後，主控台重新閱讀並**再問一輪**可能遺漏的細節：

- 根據功能清單，每項功能的邊界條件？
- 資料格式與驗證規則？
- 錯誤處理策略？
- 有無第三方整合（OAuth、Payment、外部 API）？

更新 `docs/project_overview.md`。

---

## Phase 2：模組拆分與詳細規格

### 2-1 拆分模組

根據 `project_overview.md`，主控台將專案拆分為獨立模組：

**拆分原則：**
- 每個模組對應一個子 Agent 的工作量（1-3 個程式檔案）
- 模組間介面明確（清楚定義輸入/輸出）
- 避免循環相依

**模組數量建議：**
- ≤ 8 個模組 → 全部並行
- > 8 個模組 → 詢問用戶是否分批執行

向用戶展示模組清單，**確認後**繼續。

### 2-2 相依性分析

分析模組間的相依關係，建立執行順序：

```
無相依 → 並行執行
有相依 → 被依賴的先跑，完成後再啟動依賴它的模組
```

生成 `docs/project_detail.md`：

```markdown
# [專案名稱] - 詳細規格

## 模組清單
| 模組名稱 | 負責檔案 | 相依模組 | 執行順序 |
|---------|---------|---------|---------|
| module_a | src/a.py | 無 | 第 1 批 |
| module_b | src/b.py | module_a | 第 2 批 |

## 執行批次
- 第 1 批（並行）：module_a, module_c
- 第 2 批（並行）：module_b, module_d

## 模組介面定義
[每個模組的公開 API、函式簽名、資料結構]
```

### 2-3 生成個別模組 MD

為每個模組生成 `docs/modules/[module_name].md`。

**測試情境的撰寫要求（重要）：**
測試要求欄位必須列出**具體的模擬輸入、預期輸出、以及允許的偏差範圍**，不能只寫「測試 XX 功能」這種模糊描述。格式如下：

```
測試情境 N：[情境名稱]
  輸入：[具體值或資料結構]
  預期輸出：[具體值或行為]
  允許偏差：[若有浮點數、時間戳等，說明可接受的誤差；若無則寫「無」]
  備註：[邊界條件或特別注意事項]
```

完整模組 MD 模板：

```markdown
# [模組名稱] 需求規格

## 負責檔案
- `[檔案路徑]`

## 功能描述
[這個模組要做什麼]

## 輸入 / 輸出
- 輸入：[資料格式、來源、型別]
- 輸出：[資料格式、目標、型別]

## 相依模組
- [依賴哪些其他模組，及其介面]

## 實作要求
- [具體的邏輯、演算法、規則]
- [錯誤處理方式]
- [效能要求]

## 測試要求

### 測試工具
根據專案語言與框架，選擇與技術棧相容的測試工具。
不要自行引入 overview 中未提及的新依賴。
若不確定要用什麼工具，回報主控台詢問用戶。

### 測試情境
測試情境 1：[正常流程]
  輸入：[具體值]
  預期輸出：[具體值]
  允許偏差：無

測試情境 2：[邊界條件]
  輸入：[邊界值]
  預期輸出：[具體行為，如拋出特定錯誤、回傳空值]
  允許偏差：無

測試情境 3：[錯誤情境]
  輸入：[非法輸入]
  預期輸出：[錯誤類型或錯誤訊息]
  允許偏差：無

## 禁止事項
- [不應該做的事，避免 agent 過度實作]
- 規格有任何不清楚之處，停止開發並回報主控台
```

---

## Phase 3：子 Agent 派發

### 3-1 建立專案結構

主控台在啟動子 Agent 前，先建立基本目錄結構：

```bash
mkdir -p docs/modules tests src
```

同時，根據技術棧詢問用戶需要哪些專案設定檔（如 `pyproject.toml`、`package.json` 等），由主控台統一生成，**不讓子 Agent 自行決定**。

### 3-2 派發子 Agent

每個子 Agent 的指令模板：

```
你是負責開發 [模組名稱] 的開發者。

請閱讀以下文件：
- docs/project_overview.md（了解整體專案背景）
- docs/modules/[module_name].md（你的具體任務與規格）

你的任務：
1. 根據規格實作 [檔案路徑]
2. 建立對應的測試檔案，測試工具請依據專案技術棧選擇，不要引入 overview 中未提及的新依賴
3. 完成後回報主控台：「[模組名稱] 開發完成」並列出你建立的所有檔案

⚠️ 嚴格規則：
- 只實作你負責的模組，不要修改其他模組的檔案
- 規格中有任何不清楚的地方，立刻停止並回報主控台，不得自行假設或補充
- 測試必須涵蓋模組 MD 中所有情境（含具體輸入、預期輸出、偏差驗證）
- 不得引入規格未提到的額外功能
- 程式碼要有清楚的 docstring
```

### 3-3 主控台狀態追蹤

主控台維護一個開發狀態表，對用戶顯示：

```
開發進度：
✅ module_a  - 完成（測試通過）
🔄 module_b  - 開發中
⏳ module_c  - 等待 module_a 完成
❌ module_d  - 測試失敗，修正中
❓ module_e  - 子 Agent 有問題待確認
```

---

## Phase 4：測試與修正

### 4-1 主控台執行品質檢查

每個子 Agent 回報完成後，主控台根據**專案實際技術棧**選擇適合的工具執行檢查。

**工具選擇原則：**
- 靜態分析：選用與語言相容、專案已設定的工具
- 程式碼品質：使用用戶指定或專案已有的 linter
- 測試執行：使用子 Agent 測試檔所用的框架

若專案未設定任何品質工具，先詢問用戶偏好，**不得自行選擇**。

**執行順序（適用各語言）：**
1. 靜態分析（型別 / lint）
2. 單元測試（針對該模組）
3. 整合測試（若有跨模組相依）

### 4-2 回報錯誤給子 Agent

若有錯誤，主控台將**完整**錯誤訊息傳給對應的子 Agent，不得摘要或省略：

```
[模組名稱] 檢查發現以下問題，請修正：

[工具名稱] 完整輸出：
[逐字複製，不摘要]

修正完成後回報主控台。
⚠️ 修正時若有任何不確定的地方，回報主控台詢問，不要自行猜測。
```

### 4-3 通過條件

每個模組必須滿足：
- [ ] 靜態分析無錯誤（工具由技術棧決定）
- [ ] 程式碼品質檢查無警告（工具由技術棧決定）
- [ ] 所有測試情境通過（含模組 MD 中定義的輸入/輸出與偏差驗證）

全部模組通過後，進入 Phase 5。

---

## Phase 5：整合確認

所有模組通過後，主控台執行整合測試並向用戶報告：
- 所有模組開發完成
- 測試通過率
- 生成的檔案清單
- 建議的下一步（如：部署、文件補充）

---

## 資料夾結構規範

```
專案根目錄/
├── docs/
│   ├── project_overview.md
│   ├── project_detail.md
│   └── modules/
│       ├── [module_a].md
│       └── [module_b].md
├── src/                    # 依語言調整
├── tests/
│   └── test_[module_x].[副檔名]
└── [專案設定檔]            # 由主控台根據技術棧生成，需用戶確認
```

對於前後端分離專案，調整為：
```
├── backend/
│   ├── src/
│   └── tests/
├── frontend/
│   ├── src/
│   └── tests/
└── docs/
```

---

## 主控台行為規範

1. **永遠不直接寫程式碼** — 主控台只協調、派發、檢查
2. **隨時對用戶說明狀態** — 用戶應該知道每一步在發生什麼
3. **錯誤要完整傳遞** — 不要摘要錯誤，把完整輸出給子 Agent
4. **確認再行動** — 在 Phase 2 結束後，展示模組清單讓用戶確認再開始開發
5. **模組過多時詢問** — 超過 8 個模組先詢問用戶是否分批
6. **不猜測用戶意圖** — 有任何模糊地帶立刻提問，不得自行補充合理假設
7. **工具選擇需確認** — 品質檢查工具若專案未指定，先問用戶再執行

---

## 快速參考：各 Phase 的主控台職責

| Phase | 主控台做什麼 | 輸出 |
|-------|------------|------|
| 1 | 問問題 | `docs/project_overview.md` |
| 2 | 拆模組、補問題、確認模組清單 | `docs/project_detail.md` + `docs/modules/*.md` |
| 3 | 確認設定檔、派發子 Agent | 進度狀態表 |
| 4 | 跑檢查、完整回報錯誤 | 各模組通過狀態 |
| 5 | 整合測試 | 完成報告 |
